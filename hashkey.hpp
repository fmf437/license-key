#ifndef HASHKEY_HPP
#define HASHKEY_HPP

// encripta e decripta a chave válida seleccionada e retorna o resultado

namespace lk {
    class HashKey
    {
    public:
        HashKey();
        ~HashKey();

    private:

    };
}

#endif // HASHKEY_HPP
