#ifndef OUTPUTKEY_HPP
#define OUTPUTKEY_HPP

// lê os dados encriptados do ficheiro binário .bin e retorna os dados

namespace lk {
    class OutputKey
    {
    public:
        OutputKey();
    };
}

#endif // OUTPUTKEY_HPP
